let varUnion: string | number;
varUnion = 45;
console.log(varUnion);
varUnion = 'Max'
console.log(varUnion);

let varLiteral: 'enable' | 'disable';
varLiteral = 'enable';
console.log(varLiteral);
varLiteral = 'disable';
console.log(varLiteral);